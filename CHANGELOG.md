# تغییرات نسخه‌ها

این فایل تاریخچهٔ تغییرات «دستیار من» است.
هر نسخه یک بخش با شمارهٔ همان نسخه دارد و متن همان بخش به‌صورت خودکار
در صفحهٔ Releases گیت‌هاب نمایش داده می‌شود.

## 1.0.25
- 💳 کارت حرفه‌ای «اعتبار سرویس هوش مصنوعی» در تنظیمات اضافه شد: موجودی و مصرف واقعی
  مستقیماً از API رسمی سرویس خوانده می‌شود (برای OpenRouter از مسیرهای /credits و /key).
- 🔄 به‌روزرسانی خودکار هنگام ورود به تنظیمات، دکمهٔ «به‌روزرسانی» دستی، و به‌روزرسانی
  خودکار بعد از هر درخواست هوش مصنوعی موفق.
- 🚫 هیچ مقدار ساختگی یا کم‌کردن حدسی وجود ندارد؛ اگر سرویس قابلیت دریافت موجودی نداشته
  باشد، به‌جای عدد جعلی نوشته می‌شود: «موجودی این سرویس از طریق API قابل دریافت نیست.»
- ⚠️ هشدار اعتبار کم وقتی موجودی به ۵ دلار یا کمتر برسد و هشدار قرمز وقتی موجودی به صفر
  برسد، همراه با راهنمای واردکردن API جدید در همان صفحه.
- 🔑 بخش «API جایگزین» حفظ شد؛ بعد از تست موفق، کلید جدید جای سرویس فعال را می‌گیرد.

## 1.0.24
- 📐 اصلاح چیدمان کارت‌های «قد و وزن» و «چرخه پریود»: ارتفاع ثابت که باعث می‌شد متن‌ها
  بریده شوند یا زیر کادر بیفتند حذف شد؛ حالا هر دو کارت به‌اندازهٔ هم و درست‌اندازهٔ
  محتوای خودشان هستند و هیچ نوشته‌ای بریده یا جابه‌جا نمی‌شود.
- 📊 کامل‌شدن نمودار روند: هر شاخص حالا روی بازهٔ واقعی خودش مقیاس می‌شود، پس خط‌ها
  به‌جای چسبیدن به لبه، واضح و کامل دیده می‌شوند؛ برچسب‌های تاریخ هم دقیقاً زیر نقاط
  ابتدا، میانه و انتهای نمودار می‌نشینند.
- ✅ به‌صورت پیش‌فرض همهٔ شاخص‌های دارای داده روشن نمایش داده می‌شوند تا نمودار از روز
  اول پُر و کامل باشد.

## 1.0.23
- 🔑 اتصال API اصلی و فعال به همه قابلیت‌های هوش مصنوعی: چت دستیار، پیشنهاد روزانه،
  پیشنهاد چرخه پریود، کارت شرایط پزشکی، مشاوره پوست، مشاوره بی‌رمقی، پیشنهاد آشپزی و
  ساخت تصویر. کلید به‌صورت امن از Secret پروژه داخل Build می‌آید و در کد یا فایل عمومی نیست.
- ✅ همه قابلیت‌های بالا با یک درخواست واقعی تست شدند و پاسخ گرفتند (چت، پیشنهاد روزانه،
  چرخه، شرایط پزشکی، پوست، بی‌رمقی، آشپزی و تصویر).
- ⚙️ بخش «API جایگزین» در تنظیمات حفظ شد؛ برای آینده (تمام‌شدن اعتبار، تغییر Provider یا
  واردکردن کلید جدید) کار می‌کند و بعد از آزمایش موفق، جای Provider فعلی را می‌گیرد.
- ✨ اصلاح ریشه‌ای کارت پوست: پاسخ‌های پرسشنامه اولیه (نوع پوست، جوش، قرمزی، خشکی/چربی و
  حساسیت) حالا به‌عنوان Baseline روز اول در سیستم ثبت می‌شوند؛ کارت پوست دیگر بی‌دلیل
  «ثبت نشده» نشان نمی‌دهد و اگر واقعاً داده‌ای نباشد، همان پیام نمایش داده می‌شود.
- 🌱 کارت پوست هوشمند شد: یک خلاصه از اطلاعات واقعی پوست («وضعیت پوست: …») و زیر آن
  «پیشنهاد امروز» از هوش مصنوعی، متناسب با همان اطلاعات.
- 📊 بازسازی کامل نمودار روند: نمایش هم‌زمان شاخص‌های دارای داده (انرژی، خواب، آب، بی‌رمقی،
  پوست و وزن)، هر کدام با سری مستقل، بازهٔ ۷ روز پیش‌فرض و ۳۰ روز قابل انتخاب، راهنمای
  رنگی، روشن/خاموش کردن هر شاخص، نمایش مقدار با لمس نقطه، و بدون هیچ داده ساختگی. شاخصی
  که داده ندارد اصلاً به نمودار اضافه نمی‌شود.
- 🧭 مرتب‌سازی چیدمان: ردیف چرخه پریود و قد و وزن، سپس کارت عریض پیشنهاد چرخه، سپس پوست،
  بی‌رمقی و شرایط پزشکی، و در پایان نمودار روند.
- 📋 اطلاعات پرسشنامه اولیه به‌عنوان Day 1 در آمار ثبت می‌شود و دوباره به‌عنوان Check-in
  روزانه پرسیده نمی‌شود.

## 1.0.22
- 🩺 بازطراحی کارت «وضعیت پزشکی تو»: اول نام شرایط ثبت‌شده به‌صورت واضح («شرایط ثبت‌شده: …»)
  و بعد یک توضیح کوتاه، آموزشی و مرتبط از هوش مصنوعی: نکته مراقبتی، اطلاعات عمومی، سبک
  زندگی، علائم مهم، زمان مراجعه به پزشک و اطلاعات عمومی درمان‌های شناخته‌شده.
- 🔄 محتوای کارت شرایط هر بار موضوع جدیدی را پوشش می‌دهد و از تکرار بی‌دلیل پرهیز می‌کند.
- 🔗 هوش مصنوعی فقط وقتی ارتباط منطقی وجود دارد، شرایط ثبت‌شده را به خواب، آب، تغذیه،
  انرژی، بی‌رمقی، پوست، چرخه پریود و فعالیت روزانه کاربر مرتبط می‌کند.
- 🌱 پیشنهاد روزانه هوشمندتر شد: از سن، قد، وزن، شرایط پزشکی، چک‌این‌های قبلی، خواب، آب،
  انرژی، پوست، چرخه پریود و الگوهای آموخته‌شده از گفتگوها ساخته می‌شود.
- 📈 پیشنهاد روزانه دیگر متن ثابت نیست: اگر وضعیت امروز بهتر از روز قبل باشد لحن تشویقی و
  اگر بدتر باشد لحن محتاطانه و حمایتی دارد؛ خط چرخه هم بر اساس مرحله فعلی تغییر می‌کند.
- 🛡 ایمنی: هوش مصنوعی بیماری جدید تشخیص نمی‌دهد، علت قطعی علائم را اعلام نمی‌کند، دارو
  تجویز نمی‌کند و درمان شخصی قطعی پیشنهاد نمی‌دهد؛ در موارد مهم کاربر به پزشک ارجاع داده می‌شود.
- 🧯 پایداری در قطعی سرویس: اگر هوش مصنوعی در دسترس نباشد، برنامه کرش نمی‌کند، داشبورد از
  کار نمی‌افتد، پیام خطای واضح فارسی نمایش داده می‌شود و متن ذخیره‌شده قبلی و پیشنهادهای
  محلی همچنان قابل مشاهده می‌مانند.

## 1.0.21
- 🩷 هم‌اندازه‌کردن کامل کارت «چرخه پریود» با کارت «قد و وزن»: عرض، ارتفاع، حاشیه داخلی و
  محل قرارگیری محتوا یکسان شدند و ارتفاع کارت چرخه دیگر بیشتر نمی‌شود.
- 🧮 محاسبهٔ دقیق‌تر چرخه از اطلاعات واقعی ثبت‌شده: روز چرخه، مرحلهٔ چرخه و روزهای مانده تا
  پریود بعدی، همه به‌صورت داینامیک.
- 🌸 نام‌گذاری فارسی مراحل چرخه: مرحله قاعدگی، مرحله فولیکولی، مرحله تخمک‌گذاری و مرحله
  لوتئال، همراه با توضیح کوتاه هر مرحله. در بازهٔ احتمالی تخمک‌گذاری، عبارت «تخمک‌گذاری»
  جدا و واضح نمایش داده می‌شود.
- 🔮 نمایش پیش‌بینی پریود بعدی به‌صورت تقریبی؛ اگر داده کافی نباشد، به‌جای حدس‌زدن پیام
  «اطلاعات کافی برای محاسبه دقیق وجود ندارد» نشان داده می‌شود.
- 🌸 کادر جدید «پیشنهاد امروز برای چرخه تو» بلافاصله زیر کارت‌های چرخه و قد/وزن، با متن
  متناسب با همان روز از چرخه که هر روز تغییر می‌کند و در صورت دسترسی به هوش مصنوعی
  شخصی‌سازی می‌شود.
- 🔔 یادآوری محلی پریود: ۷ روز و ۳ روز و ۱ روز قبل از پریود بعدی، بدون نیاز به اینترنت،
  با کارکرد پس از بسته‌شدن برنامه و پس از ری‌استارت گوشی، و بدون اعلان تکراری. متن اعلان
  قطعی نیست و از عبارت «احتمالاً» استفاده می‌کند. در تنظیمات قابل روشن/خاموش کردن است.
- 📊 بازطراحی نمودار روند: نمایش هم‌زمان چند شاخص (انرژی، خواب، آب، بی‌رمقی، پوست) با
  خطوط مستقل رنگی، امکان روشن/خاموش کردن هر شاخص، بازهٔ ۷ روز پیش‌فرض و ۳۰ روز قابل
  انتخاب، و فقط داده‌های واقعی ثبت‌شده بدون مقدار ساختگی برای روزهای خالی.

## 1.0.20
- 📝 افزودن فایل تاریخچهٔ تغییرات (همین فایل) و اتصال آن به ساخت خودکار نسخه‌ها.
- 🔄 از این نسخه به بعد، توضیحات فارسی هر نسخه به‌صورت خودکار در صفحهٔ Releases نوشته می‌شود.

## 1.0.19
- 📝 افزودن فایل تاریخچهٔ تغییرات (همین فایل) و اتصال آن به ساخت خودکار نسخه‌ها.
- 🔄 از این نسخه به بعد، توضیحات فارسی هر نسخه به‌صورت خودکار در صفحهٔ Releases نوشته می‌شود.

## 1.0.18
- 🔤 اصلاح کامل اعداد: همهٔ اعداد رابط کاربری (تاریخ، ساعت، وزن، قد، آب، خواب، درصد انرژی،
  روز چرخه، تعداد روزها، نمودار و تنظیمات) با اعداد فارسی ۰ تا ۹ نمایش داده می‌شوند.
- 🧭 راست‌چین‌سازی واقعی کل برنامه: قفل زبان روی فارسی و اجبار جهت راست‌به‌چپ در کل رابط
  کاربری، مستقل از زبان گوشی. همهٔ کارت‌ها، ستون‌ها، دکمه‌ها و نوار پایین آینه شدند.
- 📅 مرتب‌سازی هدر: نام، تاریخ و روز چرخه از هم جدا شدند و هر مقدار در کادر مستقل خودش
  نمایش داده می‌شود تا اعداد و متن به هم نچسبند.
- ⚖️ اصلاح کارت قد و وزن: به‌جای «۶۲.۰ کیلو • تاریخ»، وزن و آخرین ثبت در ردیف‌های جداگانه
  با برچسب فارسی قرار گرفتند.
- 💧 نسبت آب از «۵ / ۹» به «۵ از ۹» تغییر کرد تا در حالت راست‌به‌چپ درست خوانده شود.
- 🧹 حذف جداکننده‌هایی که ترتیب اعداد را در RTL به هم می‌ریختند.

## 1.0.17
- 🔤 نمایش اعداد فارسی در فیلدها و کارت‌ها و حذف صفر اضافی از فیلدهای عددی.
- ⚙️ انتقال آیکون تنظیمات به کادر خوش‌آمدگویی بالای داشبورد.
- 👋 خوش‌آمدگویی متنی داینامیک با نام کاربر و بر اساس ساعت روز.
- 🔇 حذف کامل خوش‌آمدگویی صوتی از پرسشنامه و تنظیمات.
- 🩷 هم‌اندازه‌کردن کارت چرخهٔ پریود با کارت کناری.
- 🩺 کارت شرایط پزشکی پویا شد و با هوش مصنوعی هر بار موضوع جدیدی نمایش می‌دهد.
- 🌱 پیشنهاد روزانه بر اساس داده‌های واقعی همان روز ساخته می‌شود.
- 🔑 افزودن کادر «API جایگزین» با پشتیبانی از چند سرویس و آزمایش پیش از ذخیره.
- 🧭 مرتب‌سازی داشبورد و تنظیمات.

## 1.0.16
- ✅ اصلاح صفحهٔ Releases و افزودن لینک دائمی «آخرین نسخه».
- 🔐 تثبیت کلید امضا برای آپدیت بدون پاک شدن اطلاعات.

## 1.0.11
- 💡 رفع خطای «اعتبار تمام شده» سرویس هوش مصنوعی.
- 📝 تبدیل پرسشنامهٔ اولیه به اولین چک‌این و نمایش‌ندادن دوبارهٔ آن در همان روز.
- ⚖️ افزودن قد، وزن، شاخص توده بدنی و شرایط پزشکی.

## 1.0.10
- 🤖 اتصال به سرویس هوش مصنوعی واقعی (OpenRouter).
- 🎨 یکسان‌سازی طراحی همهٔ صفحات.

## 1.0.9
- 🩸 رفع اشکال نمایش تقویم و انتخاب شدت درد.

## 1.0.8
- 🌱 نسخهٔ اولیهٔ اپلیکیشن.
